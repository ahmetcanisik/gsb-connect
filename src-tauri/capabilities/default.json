{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. App-defined commands (load_config, connect, etc.) are callable without explicit permissions; only the core defaults are listed here for events, window, path, and app APIs used by the frontend.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
